# Course3Assignment

1. script reads the label files
2. script reads test data and assigns labels to it
3. script reads train data and assigns labels to it
4. script combines test and train data
5. script aggregates the mean values of the data by Activity and Subject ID and outputs a table
